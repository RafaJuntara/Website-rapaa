RAP // PERSONA-INSPIRED UI

Standalone HTML/CSS/JS. Inspired by bold diagonal game-menu UI, not using Persona game assets.

OPTIONAL IMAGES TO ADD:
images/hero.png       = large character/hero image on HOME
images/profile.png    = portrait on ABOUT
images/project1.png   = RAP.WAV project card
images/project2.png   = second project card
images/project3.png   = third project card

OPTIONAL AUDIO:
music/bgm.mp3         = background music

If an image is missing, the page uses a built-in fallback, so the site still works.
Open index.html directly or host the whole folder on GitHub Pages.
